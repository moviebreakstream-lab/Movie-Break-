services:
  - type: web
    name: movie-break-service
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python main.py"
    envVars:
      - key: TMDB_API_KEY
        sync: false
      - key: PYTHONUNBUFFERED
        value: "1"
