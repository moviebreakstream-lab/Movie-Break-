services:
  - type: worker
    name: movie-break-worker
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python main.py"
    envVars:
      - key: TMDB_API_KEY
        sync: false
      - key: PYTHONUNBUFFERED
        value: "1"
